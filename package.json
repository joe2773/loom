{
  "name": "loom",
  "version": "0.1.0",
  "description": "Lightweight browser-based screen recorder",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "vitest": "^2.0.0",
    "jsdom": "^25.0.0"
  }
}
